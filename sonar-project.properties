sonar.projectKey=bug-tracking
sonar.projectName=Bug Tracking
sonar.sourceEncoding=UTF-8

sonar.sources=backend,frontend/src
sonar.tests=backend/tests,frontend/tests
sonar.test.inclusions=backend/tests/**/*.test.js,frontend/tests/**/*.test.mjs

sonar.exclusions=backend/tests/**,**/node_modules/**,**/dist/**,**/coverage/**,**/*.log,**/.env,**/.env.*,backend/uploads/**,deploy/mongo/backup/**

git stash
git pull origin main
git stash pop

docker system prune -a -f

docker system prune -a --volumes -f
docker compose build --no-cache